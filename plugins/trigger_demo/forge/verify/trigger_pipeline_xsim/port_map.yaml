format_version: '1'
generated_by: forge gen-top --mode verilog
source_file: /data3/home/pleguina/work/omtf-integration-test/workspace/arc-framework/gen-top/design_trigger_demo_pipeline/algo_top.v
top_module: algo_top
port_signature_hash: 5334c6f79077de61
port_groups:
  clock_reset: []
  bx0: []
  outputs:
  - name: tout_out_valid
    direction: output
    width: 1
    group: other
  - name: tout_trigger_word
    direction: output
    width: 32
    group: other
  unclassified:
  - name: ap_clk
    direction: input
    width: 1
  - name: ap_rst
    direction: input
    width: 1
  - name: dec_0_raw_hit
    direction: input
    width: 32
  - name: dec_0_raw_valid
    direction: input
    width: 1
  - name: dec_1_raw_hit
    direction: input
    width: 32
  - name: dec_1_raw_valid
    direction: input
    width: 1
  - name: dec_2_raw_hit
    direction: input
    width: 32
  - name: dec_2_raw_valid
    direction: input
    width: 1
  - name: dec_3_raw_hit
    direction: input
    width: 32
  - name: dec_3_raw_valid
    direction: input
    width: 1
tier2_probes:
  # Real probe declarations using
  # real top-level port names already listed above (dec_0_raw_valid,
  # tout_out_valid — the same input/output valid pair
  # forge.analyze.latency_runtime.measure_latency needs to measure
  # end-to-end pipeline latency). forge.verify.gen_sim's
  # _render_probe_open/_render_probe_fwrite will emit these into
  # algo_top_probe.csv in the WIDE format (cycle,<name1>,<name2>,...)
  # under --probe-log/PROBE_LOG=1 — consumed by
  # forge.analyze.latency_runtime.probe.load_wide_probe_csv.
  # XSIM execution was not run in this environment to produce a
  # real probe CSV (no Xilinx toolchain available here) — declared and
  # format-compatible, not end-to-end simulation-verified; an honest gap,
  # not a fabricated result.
  - name: dec_0_raw_valid
    net: dec_0_raw_valid
    width: 1
  - name: tout_out_valid
    net: tout_out_valid
    width: 1
